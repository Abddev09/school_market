[build]
  command = "npm ci --legacy-peer-deps && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# 🔁 Barcha sahifalarni React Routerga yo‘naltiradi
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
